# Coursework-for-COMP4030

We have implemented Logistic regression with With mode imputation in 'LR mode.ipynb' file

We have implemented SVM with With mode -1 imputation in 'SVM With -1 imputation.ipynb' file
